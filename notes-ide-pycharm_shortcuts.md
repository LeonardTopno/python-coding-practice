## Virtual Environment in VS Code 
- We do not source it.
    - `source .\.venv\Script\activate` - `cmd+F` : Search
- We just write withour the keyword source
    - `.\.venv\Script\activate`

- For Leo: create virtualenvironment using the prefix . (eg .venv or .venv_py312)
